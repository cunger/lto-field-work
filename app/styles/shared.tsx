import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  formLabel: {
    fontSize: 16,
    marginTop: 10,
    marginBottom: 10,
  }
});

export default Styles;
